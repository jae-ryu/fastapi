[pytest]
log_cli = true
log_level = NOTSET
log_format = [%(asctime)s %(threadName)s, %(levelname)s] %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
addopts = --cov-config=fastapi/.coveragec --cov=fastapi
env_files =
    fastapi/.test.env
markers = 
    integtest: mark a test as integration test.
